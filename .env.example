# Copy to .env and fill in only what you actually use.
# Nothing in this repo requires all of these. Most skills need none.

# ---------------------------------------------------------------------------
# FREE KEYS FIRST. Start here. Both have generous free tiers.
# ---------------------------------------------------------------------------
GROQ_API_KEY=
GEMINI_API_KEY=

# ---------------------------------------------------------------------------
# PAID. Only add these if a specific skill needs one and you accept the cost.
# ---------------------------------------------------------------------------
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# Scraping. Free tier is enough to start. Used by execution/apify_pool.py,
# which rotates across tokens when one hits its monthly quota.
# ---------------------------------------------------------------------------
APIFY_API_TOKEN=
APIFY_API_TOKEN_2=

# ---------------------------------------------------------------------------
# Voice and video skills only. Skip unless you are using them.
# ---------------------------------------------------------------------------
ELEVENLABS_API_KEY=
HEYGEN_API_KEY=

# ---------------------------------------------------------------------------
# Telegram delivery skills only.
# ---------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
